# Copy this file to .env and adjust values before running on a non-localhost machine.
# On a local machine the defaults work as-is — no .env file needed.

# ─── Host / URL ─────────────────────────────────────────────────────────────
# The hostname or IP address where the app is reachable from a browser.
# Change this when deploying to a server, e.g. APP_HOST=192.168.1.50
APP_HOST=localhost

# ─── Database ────────────────────────────────────────────────────────────────
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_PASSWORD=academy_password
